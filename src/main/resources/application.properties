spring.application.name=asraopticals
server.tomcat.max-http-response-header-size=65536

# --- DATABASE (H2 for local dev) -------------------------------------------
spring.datasource.url=jdbc:h2:file:./data/asra-db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
# H2 console (DISABLE in production \u2013 set to false)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# --- PostgreSQL (uncomment for production) ---------------------------------
# spring.datasource.url=jdbc:postgresql://YOUR_HOST/asraopticals_db
# spring.datasource.driverClassName=org.postgresql.Driver
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- THYMELEAF -------------------------------------------------------------
spring.thymeleaf.cache=false

# --- FILE UPLOAD ----------------------------------------------------------
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=30MB

# --- EMAIL -----------------------------------------------------------------
# Store these as environment variables in production!
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:zahedali00830@gmail.com}
spring.mail.password=${MAIL_PASSWORD:tffc irnt rhmp yxee}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- GOOGLE OAUTH2 ---------------------------------------------------------
# Store these as environment variables in production!
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:153002641073-kj066360uvcr2q5jbinufgna07hj52co.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=email,profile

# --- SERVER ----------------------------------------------------------------
server.port=8080
app.base-url=http://localhost:8080
# Session timeout (30 minutes)
server.servlet.session.timeout=30m

# --- STORE INFO ------------------------------------------------------------
store.name=Asra Optical Palace
store.phone=+91-8686151392
store.whatsapp=+918686151392
store.address=Hyderabad, Telangana, India
store.email=${MAIL_USERNAME:zahedali00830@gmail.com}
store.upi=asraopticals@upi

# -- LOW STOCK THRESHOLD -------------------------------------------------------
store.low-stock-threshold=5